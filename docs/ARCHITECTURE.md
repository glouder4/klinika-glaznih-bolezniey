# 🏗️ Архитектура системы "Клиника глазных болезней"

## Обзор архитектуры

Система построена на модульной архитектуре Bitrix24 с использованием принципов MVC и разделения ответственности. Основные компоненты взаимодействуют через четко определенные интерфейсы.

## 🧩 Компоненты системы

### 1. Модуль `artmax.calendar`

**Расположение**: `local/modules/artmax.calendar/`

Основной модуль системы, содержащий:
- Классы для работы с данными
- Обработчики событий Bitrix
- Логику бизнес-процессов
- Интеграцию с CRM

### 2. Компонент календаря

**Расположение**: `local/components/artmax/calendar/`

Пользовательский интерфейс системы:
- Отображение календарной сетки
- Формы создания/редактирования записей
- AJAX обработчики
- JavaScript функциональность

### 3. База данных

**Схема**: MySQL с таблицами:

```sql
artmax_calendar_events          -- События/записи
artmax_calendar_branches         -- Филиалы клиники
artmax_calendar_branch_employees -- Связь филиалов и сотрудников
artmax_calendar_timezone_settings -- Настройки часовых поясов
```

## 🏛️ Архитектурные слои

### 1. Слой представления (Presentation Layer)
- **Компонент календаря** - HTML/CSS/JS интерфейс
- **Административные страницы** - управление системой
- **AJAX обработчики** - динамические операции

### 2. Слой бизнес-логики (Business Logic Layer)
- **Calendar** - управление событиями
- **Branch** - управление филиалами
- **TimezoneManager** - работа с часовыми поясами
- **Events** - обработчики событий Bitrix

### 3. Слой данных (Data Access Layer)
- **Bitrix ORM** - работа с базой данных
- **CRM API** - интеграция с Bitrix24 CRM
- **Кэширование** - оптимизация производительности

## 🔄 Потоки данных

### Создание записи
```
Пользователь → Компонент → AJAX → Calendar::addEvent() → База данных
                ↓
            CRM интеграция → Создание контакта/сделки
```

### Отображение календаря
```
Запрос → Calendar::getEventsByBranch() → База данных → Форматирование → Отображение
```

### Управление филиалами
```
Админ → Branch::addBranch() → База данных → Обновление настроек → Синхронизация
```

## 🎯 Принципы проектирования

### 1. Разделение ответственности
- **Calendar** - только события
- **Branch** - только филиалы
- **TimezoneManager** - только часовые пояса

### 2. Инверсия зависимостей
- Классы зависят от абстракций, а не от конкретных реализаций
- Использование Bitrix API вместо прямых SQL запросов

### 3. Единая точка входа
- Все операции проходят через компонент календаря
- Централизованная обработка AJAX запросов

## 🔧 Интеграции

### Bitrix24 CRM
- Поиск контактов через `CCrmContact`
- Создание сделок через `CCrmDeal`
- Работа с мультиполями

### Система пользователей
- Интеграция с `CUser`
- Проверка прав доступа
- Получение информации о сотрудниках

### Часовые пояса
- Использование PHP `DateTime` и `DateTimeZone`
- Поддержка летнего/зимнего времени
- Автоматическое преобразование времени

## 📊 Производительность

### Кэширование
- Кэш компонентов Bitrix
- Кэш часовых поясов в `TimezoneManager`
- Оптимизация SQL запросов

### Масштабируемость
- Поддержка множественных филиалов
- Индексы в базе данных
- Пагинация результатов

## 🔒 Безопасность

### Аутентификация
- Проверка авторизации пользователя
- Валидация прав доступа к событиям

### Защита данных
- Экранирование SQL через `forSql()`
- Валидация входных данных
- Проверка CSRF токенов

## 🧪 Тестирование

### Структура тестов
- Unit тесты для классов
- Integration тесты для API
- UI тесты для компонентов

### Отладка
- Логирование в `debug_calendar_ajax.log`
- Обработка исключений
- Валидация данных

## 📈 Мониторинг

### Логирование
- События системы в Bitrix EventLog
- Отладочные логи для разработки
- Мониторинг ошибок

### Метрики
- Количество записей
- Загруженность врачей
- Статистика по филиалам

## 🔮 Будущие улучшения

### Планируемые функции
- Мобильное приложение
- Уведомления по SMS/Email
- Интеграция с внешними системами
- Расширенная аналитика

### Технические улучшения
- Миграция на современные фреймворки
- Внедрение микросервисной архитектуры
- Улучшение производительности
- Расширение API

---

*Документ актуален на: 04.10.2025*  
*Версия архитектуры: 1.0*
